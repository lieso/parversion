use crate::prelude::*;
use crate::transformation::FieldTransformation;

pub struct LLM {}

impl LLM {
    pub fn get_field_transformations(
        fields: Vec<String>,
        snippet: String,
    ) -> Vec<FieldTransformation> {
        unimplemented!()
    }
}
